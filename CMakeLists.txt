cmake_minimum_required(VERSION 3.16)

#=============================================================================
# PROJECT CONFIGURATION
#=============================================================================
# Basic project setup, versioning, and testing framework initialization
project(
  "snort"
  VERSION 0.1.0
  DESCRIPTION "A C library."
)

#-----------------------------------------------------------------------------
# BUILD MODE DETECTION
#-----------------------------------------------------------------------------
# Detect if this is a standalone build or being included as a subdirectory
string(
  COMPARE EQUAL
  "${CMAKE_CURRENT_SOURCE_DIR}" "${CMAKE_SOURCE_DIR}"
  SNORT_IS_STANDALONE_BUILD
)


#-----------------------------------------------------------------------------
# COMPILER-SPECIFIC SETTINGS
#-----------------------------------------------------------------------------
# Configure compiler flags and options for different toolchains
set(SNORT_DEFAULT_BUILD_TYPE "Debug")

if (MSVC)
  set(SNORT_COMPILE_OPTIONS
    /W4
    # add new msvc compile options here...
  )
else()
  set(SNORT_COMPILE_OPTIONS
    -Wall
    -Wextra
    -Wpedantic

    -Wno-gnu-zero-variadic-macro-arguments
    # add new clang or gnu compile options here...
  )
endif()

#-----------------------------------------------------------------------------
# CMAKE GLOBAL SETTINGS
#-----------------------------------------------------------------------------
# Set global CMake behavior, C standard, and build system options
set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
set(CMAKE_USE_RELATIVE_PATHS ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ${SNORT_IS_STANDALONE_BUILD})

#-----------------------------------------------------------------------------
# PROJECT OPTIONS
#-----------------------------------------------------------------------------
# Include user-configurable build options
include(CMakeOptions.txt)

#-----------------------------------------------------------------------------
# BUILD TYPE CONFIGURATION
#-----------------------------------------------------------------------------
# Set default build type for standalone builds
if (SNORT_IS_STANDALONE_BUILD AND CMAKE_BUILD_TYPE STREQUAL "")
  message("${PROJECT_NAME} | warn: Build type wasn't specified. Automatically set to \"${SNORT_DEFAULT_BUILD_TYPE}\".")
  set(CMAKE_BUILD_TYPE ${SNORT_DEFAULT_BUILD_TYPE})
endif()


#-----------------------------------------------------------------------------
# BUILD INFORMATION DISPLAY
#-----------------------------------------------------------------------------
# Print project information and configuration summary
include(CMakeInfo.txt)

#-----------------------------------------------------------------------------
# SOURCE FILES CONFIGURATION
#-----------------------------------------------------------------------------
# Define source files for the library
set(SNORT_SOURCE_FILES
  src/logging.c
  # add new source files here...
)

#=============================================================================
# LIBRARY TARGET CREATION
#=============================================================================
# Create the main library target with appropriate configuration
if(SNORT_BUILD_SHARED)
  add_library(snort SHARED ${SNORT_SOURCE_FILES})
  target_compile_definitions(snort PUBLIC __SNORT_SHARED__)
  target_compile_definitions(snort PRIVATE __SNORT_EXPORT__)
else()
  add_library(snort STATIC ${SNORT_SOURCE_FILES})
endif()

target_include_directories(snort PUBLIC include)
target_include_directories(snort PRIVATE .)

target_compile_options(snort PRIVATE ${SNORT_COMPILE_OPTIONS})

if(CMAKE_BUILD_TYPE STREQUAL "Debug")
  target_compile_definitions(snort PUBLIC __SNORT_DEBUG__)
endif()

if(SNORT_BUILD_EXAMPLE)
  add_subdirectory(example)
endif()

if(SNORT_BUILD_TESTS)
  enable_testing()
  add_subdirectory(tests)
endif()
